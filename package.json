{
  "name": "simplevideo",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "install": "cmake -B build && cmake --build build --config Release"
  },
  "devDependencies": {
    "@types/node": "^24.8.1"
  }
}
